'use strict';

var assert = require('assert');
var hasAttributeSelector = require('..');

describe('has-attribute-selector', function() {

  it('should do something awesome', function() {
    assert.equal(hasAttributeSelector(), true);
  });
});
